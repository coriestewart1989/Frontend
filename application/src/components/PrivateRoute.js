// import React from react;
// import Route, Redirect from react-router-dom;

// PrivateRoute function
//// takes arguments:
////// component, ...rest (as object)
//// and returns a Route component and is passed ...rest as self-titled prop
//// and a render prop that is supplied with a callback function that checks if
//// token exists in localStorage and if true returns the Component(itself
//// receives the props from anon-arrow fn's arguments spread into it) and if
//// false, returns Redirect to root url(or login)
